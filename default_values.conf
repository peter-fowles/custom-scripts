#!/bin/bash

# Default authors to filter by.
# Use full names or email addresses.
# For a shared repository, this list is intentionally left empty.
# Users should add their own authors to a local copy if needed.
default_authors=(
)

# Default directories to ignore during the search.
default_ignored_dirs=(
  "node_modules"
  "vendor"
  "build"
  ".venv"
  ".config"
)